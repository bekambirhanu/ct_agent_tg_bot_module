[project]
name = "ct-agent-tg-bot-module"
version = "0.1.0"
description = "Telegram bot service for trade signal intake"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"python-telegram-bot>=20.0",
	"pydantic>=2.0",
]